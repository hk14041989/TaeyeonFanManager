@model TaeyeonFanManagerSite.Models.Fan

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Fan</h4>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.LastName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.LastName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.FirstMidName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FirstMidName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.JoinedDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.JoinedDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.JoinedDates)
        </dt>

        <dd>
            <table class="table">
                <tr>
                    <th>Offline Title</th>
                    <th>Rank</th>
                </tr>
                @foreach (var item in Model.JoinedDates)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Offline.Title)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Rank)
                        </td>
                    </tr>
                }
            </table> 
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
